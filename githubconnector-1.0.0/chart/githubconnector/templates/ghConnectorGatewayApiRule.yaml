apiVersion: gateway.kyma-project.io/v1alpha1
kind: APIRule
metadata:
  name: gc-webhook-gateway
  namespace: gh-connector
  labels:
    app: github-connector
spec:
    gateway: kyma-gateway.kyma-system.svc.cluster.local
    service:
      name: gc-webhook-gateway
      port: 8081
      host: gh-webhook-endpoint.c-0398cd7.kyma-stage.shoot.live.k8s-hana.ondemand.com
    rules:
      - path: /webhook
        methods: ["POST"]
        accessStrategies:
          - handler: noop
